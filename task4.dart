/***************
* ! Task 4

* ? Title
** Replace a substring of a string

* ? Details
** Given three strings stack, needle, and newNode, the task is to modify the string stack by replacing all the substrings needle with the string newNode in the string stack.

* ? Example 
** input (stack: 'welcome#to#dart', needle: '#', newNode: ' ')
** output 'welcome to dart'
*/

String replaceAllSubstring(String stack, String needle, String newNode) {
  for(int i =0;i< stack.length;i++){
  
  }
  return '';
}
void main (){
  String stack ='welcome#to#dart';
  print(stack.replaceAll('#', ''' '''));
}
